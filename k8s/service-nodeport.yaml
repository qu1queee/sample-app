apiVersion: v1
kind: Service
metadata:
  name: cc-server-nodeport
  namespace: cc-week08
spec:
  type: NodePort
  selector:
    app: cc-server
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    # nodePort is omitted — Kubernetes assigns one automatically in the 30000–32767 range
