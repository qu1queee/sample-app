apiVersion: v1
kind: Service
metadata:
  name: cc-server-svc
  namespace: cc-week09
spec:
  type: ClusterIP
  selector:
    app: cc-server
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
