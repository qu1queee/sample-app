apiVersion: v1
kind: Service
metadata:
  name: cc-server
  namespace: cc-week11
spec:
  type: ClusterIP
  selector:
    app: cc-server
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
